1. 우선 수열을 완전이진트리로 만들기 위해 0을 패딩해준다.
2. 그 다음 최솟값 완전 이진 트리를 생성한다. (여기서 트리는 수열의 값이 아닌 포인터를 들고 있어야 한다.)
3. 이제 준비는 끝났다. 구간의 최솟값을 구하고, 최솟값*구간의 길이를 최댓값과 비교하여 갱신하고, 최솟값의 포인터를 기준으로 구간을 두개로 분할해나간다.
4. 구간의 길이가 1이 될때까지 무한 반복해서 최댓값을 구한다!