그냥 스택 쓰면 되는거 아닐까?
가다가 찾고자 하는 문자열을 S, 전체 문자열을 T라고 하면
S[stack.count]와 T(n)을 비교, 둘이 같으면 S[stack.count+1]을 stack에 넣고 다음 인덱스로.
만약 stack.count == m-1이라면 정답 큐에 얘를 넣고 스택 초기화 시키면 끝. 쉬운데? 바로 해보자.

잠깐. 이러면 굳이 스택을 안써도 되는거 아니야? stack.count가 아니라 그냥 정수 변수 하나 만들어서 그거 1씩 올리면서 하면 되잖아.